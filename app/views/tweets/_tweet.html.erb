<div class='well'>
  <strong><%= link_to tweet.user.username, show_user_path(id: tweet.user_id) %></strong> says...
  <span class="pull-right"><%= tweet.created_at %></span>
  <p>
    <%= tweet.message.html_safe %>
  </p>
</div>
