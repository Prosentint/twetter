<div class='col-md-4'>
  <div class="well">
    <h3><%= link_to user.username, show_user_path(id: user.id), class: 'link' %></h3>
    <h3><%= user.name %></h3>
    <h4>
      Location: <%= user.location %>
    </h4>
    <%= render 'tweets/tweet', tweet: user.tweets.last %>
  </div>
</div>
